import visualizeMovement from "../src/visualization.js"
describe('visualizeMovement function', () => {
    it('should be defined', () => {
        expect(visualizeMovement).toBeDefined();
    });

    // Vous pouvez ajouter d'autres tests en fonction de ce que fait votre fonction.
    // Par exemple, si votre fonction renvoie quelque chose, vous pouvez tester cette valeur de retour.
    // Si elle modifie le DOM, vous pouvez utiliser des outils comme `jest-dom` pour tester ces modifications.
});
